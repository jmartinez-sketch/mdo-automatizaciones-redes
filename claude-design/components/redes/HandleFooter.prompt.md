Pie de placa. Es lo ultimo que va dentro de `<Plate>`.

```jsx
<HandleFooter handle="@mdoconsultores" onInverse />
<HandleFooter handle="@mdoconsultores" right="Desde 1972 · Buenos Aires" />
```

Los dos textos van con `nowrap`: el defecto historico era que el pie se partia en dos lineas.
